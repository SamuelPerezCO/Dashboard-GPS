"""Registro de modelos de tracking en el admin de Django.

La app no define modelos propios (todos los datos vienen del API de
Service24GPS en tiempo real), por lo que no hay nada que registrar aquí.
"""

from django.contrib import admin
