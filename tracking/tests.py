"""Pruebas de la app tracking.

Aún no hay pruebas automatizadas; este módulo queda listo para alojar
TestCases que ejerciten :mod:`tracking.services` y
:mod:`tracking.api_client` (por ejemplo, simulando respuestas del API).
"""

from django.test import TestCase
